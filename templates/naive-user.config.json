{
  "app": "myapp",
  "baseUrl": "http://localhost:3000",
  "startCommand": null,
  "auth": {
    "steps": [
      "Go to /",
      "If bounced to a login page, type the dev username into the username field",
      "Submit to land back in the app"
    ],
    "critiqueLoginPage": false
  },
  "browser": { "device": "desktop" },
  "commitFindings": "ask",
  "coverageNotes": "Optional hints, not a script. e.g. 'top nav, the create form, the results list, the item detail/edit/delete, search, empty + loading states'."
}
